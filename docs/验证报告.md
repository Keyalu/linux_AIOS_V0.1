## 第一档：纯工具调用（不依赖 GUI，最稳）

表格

| #    | 命令                                                         | 验证功能点          | 预期结果                                     |
| ---- | ------------------------------------------------------------ | ------------------- | -------------------------------------------- |
| 1    | `python3 main.py --text "查询长沙天气"`                      | 信息查询 / MCP 工具 | 一步 `mcp_weather`，结果含 "长沙"，审计 PASS |
| 2    | `python3 main.py --text "查看磁盘使用情况"`                  | disk_usage          | 工具步骤成功，返回各分区用量                 |
| 3    | `python3 main.py --text "检查一下系统状态"`                  | system_check        | 返回 CPU / 内存 / 进程概览                   |
| 4    | `python3 main.py --text "打开 https://www.bing.com"`         | open_url            | 浏览器真打开该网址                           |
| 5    | `python3 main.py --text "写一个文件 /tmp/aios_test.txt 内容是 hello"` | write_file          | `/tmp/aios_test.txt` 真落盘                  |

## 第二档：文件操作（真实工具，沙箱目录）

表格

| #    | 命令                                                         | 验证功能点       | 预期结果                                    |
| ---- | ------------------------------------------------------------ | ---------------- | ------------------------------------------- |
| 6    | `python3 main.py --text "帮我把 demo_task 文件夹里的文件整理一下"` | organize         | 文档 / 代码 / 图片分类归拢，返回 moved 计数 |
| 7    | `python3 main.py --text "看看 demo_task 里有没有重复文件"`   | find_duplicates  | 返回重复组（报告.docx/ 报告副本.docx）      |
| 8    | `python3 main.py --text "把 demo_task 备份一下"`             | backup           | 生成 `demo_task_backup.zip`，返回路径       |
| 9    | `python3 main.py --text "找出 home 目录下大于 100M 的文件"`  | find_large_files | 返回大文件列表                              |
| 10   | `python3 main.py --text "清理临时文件"`                      | cleanup_temp     | 返回清理计数                                |

## 第三档：安全沙箱

表格

| #    | 命令                                | 验证功能点      | 预期结果                                              |
| ---- | ----------------------------------- | --------------- | ----------------------------------------------------- |
| 11   | `python3 main.py --text "rm -rf /"` | SecuritySandbox | **应被拦截**，提示 "危险操作: rm -rf"，不执行任何步骤 |

## 第四档：GUI 基础（依赖 AT-SPI，需真实桌面会话）

表格

| #    | 命令                                       | 验证功能点           | 预期结果                                                     |
| ---- | ------------------------------------------ | -------------------- | ------------------------------------------------------------ |
| 12   | `python3 main.py --text "打开文件管理器"`  | open_app 短路        | **单步 click**，点 dock 上 "文件" 图标，不按 Super、不打字；文件管理器真打开 |
| 13   | `python3 main.py --text "点击label，文件"` | click_element 新功能 | 意图 =`click_element`，name = 文件 role=label；click 走合成坐标，不再 `[Mock]` |
| 14   | `python3 main.py --text "打开 Firefox"`    | open_app             | dock 有 Firefox 则直点；没有则 Super→搜索→点结果             |
| 15   | `python3 main.py --text "切换到浏览器"`    | switch_window        | Alt+Tab 类快捷键，窗口切换                                   |

## 第五档：GUI 复合动作

表格

| #    | 命令                                                        | 验证功能点      | 预期结果                                                     |
| ---- | ----------------------------------------------------------- | --------------- | ------------------------------------------------------------ |
| 16   | `python3 main.py --text "导航到 /home/keyal"`               | navigate        | Ctrl+L → 输入路径 → Enter，文件管理器跳到该目录              |
| 17   | `python3 main.py --text "在当前位置创建 test 文件夹"`       | create_folder   | Ctrl+Shift+N → 输入 test → Enter，文件夹真出现               |
| 18   | `python3 main.py --text "截个屏保存到 /tmp/shot.png"`       | take_screenshot | **等 2 秒后**截图，`/tmp/shot.png` 真生成                    |
| 19   | `python3 main.py --text "截个屏然后发到 3179160953@qq.com"` | 截图→邮件链路   | 截图落盘 → send_email 带附件；dry_run 由 "发" 字判定为真发（会真发邮件） |

## 第六档：提权流与自愈流

表格

| #    | 命令                                          | 验证功能点   | 预期结果                                               |
| ---- | --------------------------------------------- | ------------ | ------------------------------------------------------ |
| 20   | `python3 main.py --text "帮我整理一下文件"`   | 多 goal 串联 | organize + 可能的查重 / 备份依次执行，审计 PASS        |
| 21   | 在 Web 控制台跑一条带 `requires_admin` 的命令 | 提权流       | 弹出确认 → 同意后以 ADMIN 重试一次，escalations 有记录 |

